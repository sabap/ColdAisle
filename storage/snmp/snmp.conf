# ColdAisle Net-SNMP config for Windows PHP CLI (SNMPCONFPATH = this directory).
# Leave the default IETF MIB set unloaded. ColdAisle polls with numeric OIDs.
# (Do not use "mibs none" — some Net-SNMP builds treat "none" as a module name.)

mibdirs C:/invalid/mibs/path/intentionally/empty
